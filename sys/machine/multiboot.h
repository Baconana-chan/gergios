/*	$NetBSD$	*/
/* Multiboot header — stub for AArch64 (not applicable: ARM64 uses device tree/UEFI). */

#ifndef _MACHINE_MULTIBOOT_H_
#define _MACHINE_MULTIBOOT_H_

/* AArch64 does not use the Multiboot boot protocol. */
/* Boot information is provided via device tree or UEFI. */

#endif /* _MACHINE_MULTIBOOT_H_ */
