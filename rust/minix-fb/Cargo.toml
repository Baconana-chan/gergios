[package]
name = "minix-fb"
version = "0.1.0"
edition = "2021"
description = "MINIX framebuffer backend — software and hardware display support"
license = "BSD-3-Clause"

[dependencies]
minix-compositor = { path = "../minix-compositor" }

[features]
default = []
